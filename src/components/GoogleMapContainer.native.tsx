// Native stub — Google Maps JavaScript API is web-only.
// Metro automatically uses this file on iOS/Android instead of
// GoogleMapContainer.tsx, preventing any DOM/browser imports from bundling.
export default function GoogleMapContainer() {
  return null;
}
