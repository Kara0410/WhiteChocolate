export const MAP_LAYERS = {
  map: 0,
  markers: 10,
  userLocation: 20,
  floatingControls: 80,
  bottomSheet: 100,
  navBackdrop: 190,
  navBar: 200,
} as const;

export const MAP_ELEVATIONS = {
  map: 0,
  markers: 2,
  userLocation: 3,
  floatingControls: 8,
  bottomSheet: 12,
  navBackdrop: 18,
  navBar: 20,
} as const;
