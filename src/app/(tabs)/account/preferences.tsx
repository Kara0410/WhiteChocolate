import { AccountPlaceholderScreen } from '@/components/account/account-placeholder-screen';

export default function AccountPreferencesScreen() {
  return (
    <AccountPlaceholderScreen
      description="Language, units, and appearance are represented in the settings model. Their full controls will be added when those app features are implemented."
      title="Preferences"
    />
  );
}
