import { AccountPlaceholderScreen } from '@/components/account/account-placeholder-screen';

export default function AccountLicensesScreen() {
  return (
    <AccountPlaceholderScreen
      description="The route is ready for generated third-party acknowledgements. Legal document content is intentionally deferred."
      title="Open-source licenses"
    />
  );
}
