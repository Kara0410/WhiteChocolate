import { AccountPlaceholderScreen } from '@/components/account/account-placeholder-screen';

export default function AccountNotificationsScreen() {
  return (
    <AccountPlaceholderScreen
      description="Notification and parking-reminder preferences are stored locally. Notification permissions and delivery are intentionally outside Week 1."
      title="Notifications"
    />
  );
}
